# Content repository task graph — the authoritative build definition.
#
#   Install Task:                       choco install go-task  (or winget install Task.Task)
#   List tasks:                         task --list
#   Everything required before merging: task verify
#
# Task IS the orchestration layer. The simple verbs are defined directly here so
# the graph restores once, builds once, and tests without rebuilding. Only
# genuinely complex, multi-step behavior lives in script/*.ps1, which Task
# invokes in its own process. Run `task doctor` to check your toolchain.
#
# NOTE: YAML forbids tab indentation, so this file uses spaces. That is the one
# deliberate exception to the repository's tabs-everywhere rule.
version: '3'

# Run each task at most once per invocation. Without this, Task's default
# (run: always) would re-run shared prerequisites — e.g. `verify` calls build
# and test, and test also depends on build, so restore+build would run twice.
run: once

# Layered environment for genuine per-developer PREFERENCES only (e.g.
# configuration). Task's dotenv is first-file-wins, so the gitignored local
# override is listed first. A missing .env.local is fine. NOTE: the build TARGET
# is authoritative below in vars, NOT in .env — so every developer and agent
# verifies the same thing.
dotenv: ['.env.local', '.env']

vars:
  # Authoritative build target. Note the solution lives under src/, not at the
  # repository root.
  TARGET: 'src/Content.sln'

  # pwsh invocation for the complex scripts. -ExecutionPolicy Bypass is
  # process-scoped (it does not change machine policy) and keeps a fresh
  # agent/CI shell from failing with "running scripts is disabled".
  PWSH: 'pwsh -NoProfile -ExecutionPolicy Bypass -File'

tasks:
  default:
    desc: List available tasks
    cmds: [task --list]
    silent: true

  restore:
    desc: Restore dependencies for the whole target
    cmds:
      # NOTE: no `dotnet tool restore` here. Unlike the sibling stories and
      # digihouse repositories, this one has no .config/dotnet-tools.json, and
      # the command fails outright without a manifest. If a local tool is ever
      # added, add the manifest and this line together.
      - dotnet restore {{.TARGET}}

  build:
    desc: Build the target (fast inner loop)
    deps: [restore]
    cmds:
      - dotnet build {{.TARGET}} -c ${BRP_DOTNET_CONFIGURATION:-Debug} --nologo --no-restore

  test:
    desc: Run unit tests against the target (no rebuild)
    deps: [build]
    cmds:
      - dotnet test {{.TARGET}} -c ${BRP_DOTNET_CONFIGURATION:-Debug} --nologo --no-build --no-restore

  pack:local:
    # Packs the real packages, in Release, into a folder feed the sibling repositories restore
    # from -- instead of publishing a prerelease and waiting for nuget.org to index it. Every
    # pack gets a unique version, because NuGet's global packages cache is keyed on id and
    # version and would otherwise keep serving the copy it already unpacked.
    desc: 'Pack the NuGet packages into the shared local feed, for testing without publishing'
    cmds: ['{{.PWSH}} script/pack-local.ps1 {{.CLI_ARGS}}']

  use:local:
    # The other side: build against packages a sibling packed locally. `-- released` puts it
    # back. Being on local packages shows up in git status on purpose, and `task release`
    # refuses a dirty tree, so a local reference cannot reach a published version.
    desc: 'Use locally packed BigRedProf packages. Usage: task use:local -- <version>|released'
    cmds: ['{{.PWSH}} script/use-packages.ps1 -Version {{.CLI_ARGS}}']
    preconditions:
      - sh: '[ -n "{{.CLI_ARGS}}" ]'
        msg: 'A version is required. Usage: task use:local -- <version>|released'

  verify:
    desc: Everything required before merging (the canonical success criterion)
    cmds:
      - task: build
      - task: test

  clean:
    desc: Remove build outputs and artifacts
    cmds:
      - dotnet clean {{.TARGET}} --nologo
      - pwsh -NoProfile -Command "Remove-Item -Recurse -Force artifacts -ErrorAction SilentlyContinue"

  doctor:
    desc: Tool and version diagnostics (checks Task/.NET)
    cmds: ['{{.PWSH}} script/doctor.ps1']

  release:
    # Publishing stays CI's job -- nothing here pushes a package. What this pushes is the
    # `v*` tag that makes CI publish, which is the step that is easy to get wrong and
    # impossible to take back: nuget.org versions are immutable, so a tag on the wrong
    # commit or a typo'd version is spent forever. Hence the checks and the confirmation
    # rather than two commands to remember.
    desc: 'Release a version by tagging it, which is what publishes to nuget.org. Usage: task release -- 0.1.0'
    cmds: ['{{.PWSH}} script/release.ps1 -Version {{.CLI_ARGS}}']
    preconditions:
      - sh: '[ -n "{{.CLI_ARGS}}" ]'
        msg: 'A version is required. Usage: task release -- 0.1.0'

  pack:
    desc: Build the BigRedProf.Content.Core NuGet package locally (into artifacts/packages)
    # Depends on restore, NOT build. The package is always Release, so depending
    # on `build` would compile Debug first and then have `dotnet pack` compile
    # Release anyway -- two builds for one artifact.
    deps: [restore]
    cmds:
      - dotnet pack {{.TARGET}} -c Release --nologo --no-restore -o artifacts/packages

# -----------------------------------------------------------------------------
# NOT DEFINED HERE, DELIBERATELY
#
# image / publish — this repository ships a LIBRARY, not a service. There is no
# Dockerfile and nothing to push to a container registry.
#
# Package publishing is CI's job. .github/workflows/dotnet.yml pushes
# BigRedProf.Content.Core to nuget.org when a `v*` TAG is pushed -- not on a
# merge to main; `task pack` only builds it locally, so nothing here can release
# a package by accident. To release, use `task release` -- it pushes that tag
# after checking everything that cannot be undone once it exists. MinVer reads
# the version from the tag (Directory.Build.props), so there is no version to
# bump in any csproj. The workflow calls
# `task verify` for the build-and-test half, so CI and local agree on what
# "it builds" means.
# -----------------------------------------------------------------------------
