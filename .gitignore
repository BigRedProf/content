# Build results
bin/
obj/

# Visual Studio
.vs/
*.user

# Test results
TestResults/

# BigRedProf: generated build outputs (task clean removes this tree)
artifacts/

# BigRedProf: per-developer environment overrides (Task loads this before .env)
.env.local

# Which locally packed packages this repository is pointed at (task use:local).
.local-packages.json
